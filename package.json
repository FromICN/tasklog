{
  "name": "tasklog",
  "private": true,
  "version": "1.0.0",
  "description": "TaskLog — Firebase 실시간 동기화 + Capacitor APK",
  "scripts": {
    "build:www": "node copy-www.js",
    "android": "npm run build:www && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^6.1.0",
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "firebase": "^10.14.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
